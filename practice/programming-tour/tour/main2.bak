package main

import (
	"flag"
	"log"
)

func main() {
	var name string
	flag.StringVar(&name, "name", "go programming tour", "help info")
	flag.StringVar(&name, "n", "go programming tour", "help info")
	flag.Parse()
	log.Printf("name:%s", name)
}
